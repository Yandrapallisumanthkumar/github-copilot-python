# Sudoku Starter

This is a small Flask-backed Sudoku project.

## Testing

This project uses `pytest` for automated tests. Run the test suite with the following command on Windows:

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```
